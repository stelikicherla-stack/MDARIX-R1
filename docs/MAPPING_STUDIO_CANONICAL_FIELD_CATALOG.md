# Canonical Field Catalog

Canonical entities and fields are controlled by the backend. Target fields are selected only after an entity is selected. System-managed and protected fields are explicitly marked `PLATFORM_ONLY` and `mapping_allowed=false`.
