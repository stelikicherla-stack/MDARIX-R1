# Mapping Studio — AI Suggestions

Suggestions are optional advisory results. Each suggestion is labelled `AI_SUGGESTION_NOT_ACTIVE` and cannot activate a mapping. A human must explicitly accept, modify, or reject it; the decision is recorded with the authenticated context and remains separate from source business-object changes.

No provider credential is required for the deterministic similarity simulator. Live GenAI execution is an external validation gate.
