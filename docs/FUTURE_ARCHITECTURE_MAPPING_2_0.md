# Stage 2 — Mapping 2.0

Mapping remains version-pinned: canonical model → connector/source definitions → master family/version/items → transformations and validation → tenant mapping version and permitted overrides. Published master versions are immutable and tenant activation is explicit; platform-owned tenant, audit, provenance, and source identity fields are protected.
