# Unknowns Radar Architecture

The radar derives unknowns only from bounded hypothesis gaps, assumptions, temporal limitations, and material Day 13 challenges. It does not query or expose an unrestricted database dump, and it never fills missing data with an AI guess. Current and known-as-of context remain represented by the upstream snapshot.
