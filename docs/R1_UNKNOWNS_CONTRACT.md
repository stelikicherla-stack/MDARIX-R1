# Unknowns Radar Contract

`UnknownSet` contains canonical `InvestigationUnknown` records with category, materiality, why-it-matters, resolution requirement/source type, temporal context, source references, provenance, and human-review state. Deduplication is semantic within a controlled execution. `unknown_is_not_false` and `no_invented_resolution` are persisted guardrails.
