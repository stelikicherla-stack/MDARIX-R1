# Initial Master Mapping Families

Stage 1 publishes the required TrackWise, Veeva, PLM, SAP, Salesforce, Risk, RIM, Safety, LIMS, and MES mapping family codes through `/api/v1/admin/mapping/families`. Existing durable master-mapping tables remain the versioned persistence mechanism; this stage supplies the metadata contracts without duplicating those tables.
