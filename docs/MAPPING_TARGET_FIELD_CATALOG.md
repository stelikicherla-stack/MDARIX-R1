# MDARIX R1 — Target Field Catalog

Canonical target fields are dependent on the selected canonical entity. Each field declares type, required/nullable status, reference entity, validation rule, sensitivity, system-managed status, mapping allowance, and override policy.

Protected targets are platform-only and are excluded from ordinary mapping selection or rejected server-side. Tenant identity and authorization metadata are never accepted from source payloads.
