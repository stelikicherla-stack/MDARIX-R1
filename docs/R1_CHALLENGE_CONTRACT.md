# R1 Challenge Contract

`ChallengeSet` is the Day 14 handoff. It contains `InvestigationChallenge` records, context snapshot/version, source hypothesis set, validation summary, guardrails, provenance, and explicit `no_material_challenge` / `abstained` states. Challenge types and statuses are controlled literals in `challenger/schemas.py`.
